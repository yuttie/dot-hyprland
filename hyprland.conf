# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/

# Define a fallback rule
monitor = , preferred, auto, 1

monitor = OFFSCREEN, preferred, 10000x0, 1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal   = ~/.local/bin/term
$editor     = $terminal -- sh -c 'sleep 0.05; exec nvim'
$webbrowser = /usr/bin/firefox
$webbrowser_private = /usr/bin/firefox --private-window
$subwebbrowser = /usr/bin/google-chrome-stable
$rofi       = rofi -no-case-sensitive -matching regex -sort -sorting-method fzf
$menu       = $rofi -modi drun##run -show drun -show-icons -drun-show-actions


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_THEME, Breeze
env = XCURSOR_SIZE, 32
env = HYPRCURSOR_SIZE, 32

env = XDG_SESSION_TYPE,wayland
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_CURRENT_SESSION,Hyprland
env = XDG_SESSION_DESKTOP,Hyprland
env = LIBSEAT_BACKEND,logind
env = GDK_BACKEND,wayland,x11,*
env = QT_QPA_PLATFORM,wayland;xcb
env = SDL_VIDEODRIVER,wayland,x11
env = QT_QPA_PLATFORMTHEME,qt5ct  # Has effect also for Qt6 (no need to be qt6ct)
env = GDK_DPI_SCALE,1.33333
env = QT_SCALE_FACTOR,1.33333
env = MOZ_ENABLE_WAYLAND,1
env = WINIT_UNIX_BACKEND,x11
env = _JAVA_AWT_WM_NONREPARENTING,1

# Input method
env = XMODIFIERS,@im=fcitx
env = QT_IM_MODULE,fcitx
env = QT_IM_MODULES,wayland;fcitx;ibus
env = QT5_IM_MODULE,fcitx
env = SDL_IM_MODULE,fcitx
env = GLFW_IM_MODULE,ibus


#################
### AUTOSTART ###
#################

# Import specific environment variables into the systemd user session and dbus
$env-vars-to-import = DISPLAY WAYLAND_DISPLAY
$my-env-vars-to-import = XCURSOR_THEME XCURSOR_SIZE HYPRCURSOR_SIZE XDG_SESSION_TYPE XDG_CURRENT_DESKTOP XDG_CURRENT_SESSION XDG_SESSION_DESKTOP LIBSEAT_BACKEND GDK_BACKEND QT_QPA_PLATFORM SDL_VIDEODRIVER QT_QPA_PLATFORMTHEME GDK_DPI_SCALE QT_SCALE_FACTOR MOZ_ENABLE_WAYLAND WINIT_UNIX_BACKEND _JAVA_AWT_WM_NONREPARENTING XMODIFIERS QT_IM_MODULE QT_IM_MODULES QT5_IM_MODULE SDL_IM_MODULE GLFW_IM_MODULE
exec-once = systemctl --user import-environment $env-vars-to-import $my-env-vars-to-import
exec-once = dbus-update-activation-environment --systemd $env-vars-to-import $my-env-vars-to-import

# Wallpaper
exec = pidof hyprpaper || hyprpaper

# Idle daemon
exec = pidof hypridle || hypridle

# Polkit agent
exec = pidof hyprpolkitagent || sh -c '[ -x /usr/libexec/hyprpolkitagent ] && exec /usr/libexec/hyprpolkitagent || exec /usr/lib/hyprpolkitagent/hyprpolkitagent'

# Autostart
exec-once = xrdb -load ~/.Xresources
exec-once = pasystray
exec-once = nm-applet --indicator
exec-once = blueman-applet
exec-once = dropbox
exec-once = /opt/1Password/1password

exec = pidof fcitx5 || fcitx5 -dr
exec = pidof waybar || waybar

# Settings for Gnome apps
$gnome-schema = org.gnome.desktop.interface
exec = gsettings set $gnome-schema color-scheme 'prefer-light'  # Valid values are “default”, “prefer-dark”, “prefer-light”
exec = gsettings set $gnome-schema icon-theme 'WhiteSur-light'
exec = gsettings set $gnome-schema gtk-theme 'WhiteSur-Light'
exec = gsettings set $gnome-schema gtk-key-theme 'Emacs'
exec = gsettings set $gnome-schema font-name 'Sans 10'
exec = gsettings set $gnome-schema monospace-font-name 'Monospace 10'
exec = gsettings set $gnome-schema cursor-theme 'Breeze'
exec = gsettings set $gnome-schema cursor-size '32'
exec = gsettings set $gnome-schema font-antialiasing 'rgba'
exec = gsettings set $gnome-schema font-hinting 'none'
exec = gsettings set org.gnome.settings-daemon.plugins.xsettings overrides "{'Gtk/IMModule':<'fcitx'>}"


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 8
    gaps_out = 16

    border_size = 8

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(33ff7aee) 45deg
    col.inactive_border = rgba(ffffff33)

    layout = dwindle

    no_focus_fallback = true

    allow_tearing = false
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 16

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 128
        render_power = 2
        color = rgba(00000066)
        offset = 0 24
        scale = 0.95
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 8
        passes = 3
        ignore_opacity = true
        noise = 0.02
    }
}

group {
    auto_group = true
    insert_after_current = true
    focus_removed_window = true
    drag_into_group = 1
    merge_groups_on_drag = false
    merge_floated_into_tiled_on_groupbar = false
    group_on_movetoworkspace = false

    col.border_active = rgba(33ccffee) rgba(33ff7aee) 45deg
    col.border_inactive = rgba(ffffff33)

    groupbar {
        indicator_height = 16
        rounding = 8
        round_only_edges = true
        col.active = rgba(ffffff88)
        col.inactive = rgba(ffffff33)
        gaps_in = 4
        gaps_out = 8
        render_titles = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    #        NAME,           X0,   Y0,   X1,   Y1
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    animation = windows, 1, 5, default, popin 80%
    animation = fade, 1, 5, default
    animation = border, 1, 2, default
    animation = borderangle, 0
    animation = workspaces, 1, 5, default, slide
    animation = zoomFactor, 1, 5, easeOutQuint
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    force_split = 2
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
    mouse_move_enables_dpms = false
    key_press_enables_dpms = true
    focus_on_activate = true
    on_focus_under_fullscreen = 1
    exit_window_retains_fullscreen = true
    anr_missed_pings = 5
}

render {
    direct_scanout = 2
}

cursor {
    zoom_detached_camera = false
    zoom_disable_aa = true
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = jp
    kb_variant = dvorak
    kb_options = ctrl:nocaps,mod:custom
    numlock_by_default = true
    repeat_delay = 300
    repeat_rate = 60

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

binds {
    scroll_event_delay = 0
    allow_workspace_cycles = true
    ignore_group_lock = true
    window_direction_monitor_fallback = false
}


####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Launch app
bind = $mainMod, semicolon, exec, $terminal
bind = $mainMod, E, exec, $editor
bind = $mainMod, W, exec, $webbrowser
bind = $mainMod&Control_L, W, exec, $webbrowser_private
bind = $mainMod&Shift_L, W, exec, $subwebbrowser
bind = $mainMod, R, exec, $menu
bind = $mainMod, Zenkaku_Hankaku, exec, killall fcitx5; fcitx5 -dr

# Session
bind = $mainMod&Control_L, X, exec, loginctl lock-session
bind = $mainMod&Control_L, Q, exit,

# Monitor
bind = $mainMod, comma, focusmonitor, l
bind = $mainMod, period, focusmonitor, r
bind = $mainMod&Control_L, Left, focusmonitor, l
bind = $mainMod&Control_L, Right, focusmonitor, r
bind = $mainMod&Control_L, Up, focusmonitor, u
bind = $mainMod&Control_L, Down, focusmonitor, d

# Workspace
bind = $mainMod, B, workspace, previous_per_monitor
bind = $mainMod&Shift_L, Tab, workspace, m-1
bind = $mainMod, Tab, workspace, m+1
gesture = 3, left, dispatcher, workspace, m+1
gesture = 3, right, dispatcher, workspace, m-1
bind = $mainMod, Left, workspace, m-1
bind = $mainMod, Right, workspace, m+1
bind = $mainMod, mouse_left, workspace, m-1
bind = $mainMod, mouse_right, workspace, m+1
bind = $mainMod, V, exec, ~/.config/hypr/switch-workspace.sh
bind = $mainMod&Shift_L, R, exec, ~/.config/hypr/rename-workspace.sh
bind = $mainMod&Shift_L, comma, movecurrentworkspacetomonitor, l
bind = $mainMod&Shift_L, period, movecurrentworkspacetomonitor, r
bind = $mainMod&Shift_L&Control_L, Left, movecurrentworkspacetomonitor, l
bind = $mainMod&Shift_L&Control_L, Right, movecurrentworkspacetomonitor, r
bind = $mainMod&Shift_L&Control_L, Up, movecurrentworkspacetomonitor, u
bind = $mainMod&Shift_L&Control_L, Down, movecurrentworkspacetomonitor, d

# Dwindle layout
bind = $mainMod, slash, togglesplit,

# Group
bind = $mainMod, Escape, lockgroups, toggle
bind = $mainMod, T, exec, ~/.config/hypr/enable-group.sh
bind = $mainMod&Control_L, T, exec, ~/.config/hypr/disable-group.sh
bind = $mainMod, P, changegroupactive, b
bind = $mainMod, N, changegroupactive, f
bind = $mainMod, mouse_down, changegroupactive, b
bind = $mainMod, mouse_up, changegroupactive, f
bind = $mainMod&Shift_L, P, movegroupwindow, b
bind = $mainMod&Shift_L, N, movegroupwindow, f

# Focused window
bind = $mainMod&Control_L, C, killactive,
bind = $mainMod, space, focuswindow, floating
bind = $mainMod, space, alterzorder, top
bind = $mainMod&Control_L, space, focuswindow, tiled
bind = $mainMod&Shift_L, space, togglefloating,
bind = $mainMod, F, fullscreenstate, 2 -1
bind = $mainMod&Control_L, F, fullscreenstate, -1 2
bind = $mainMod&Shift_L, S, pin,

# Hiding/restoring workspaces
bind = $mainMod, backslash, exec, ~/.config/hypr/hide-workspace.sh
bind = $mainMod&Shift_L, backslash, exec, ~/.config/hypr/hide-all-except-active.sh
bind = $mainMod&Control_L, backslash, exec, ~/.config/hypr/restore-workspace.sh

# Move focus
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Move focused window
bind = $mainMod&Shift_L, H, movewindow, l
bind = $mainMod&Shift_L, L, movewindow, r
bind = $mainMod&Shift_L, K, movewindow, u
bind = $mainMod&Shift_L, J, movewindow, d
bind = $mainMod&Control_L, H, movewindoworgroup, l
bind = $mainMod&Control_L, L, movewindoworgroup, r
bind = $mainMod&Control_L, K, movewindoworgroup, u
bind = $mainMod&Control_L, J, movewindoworgroup, d
bind = $mainMod, M, exec, ~/.config/hypr/move-to-workspace-silent.sh
bind = $mainMod&Shift_L, M, exec, ~/.config/hypr/move-to-workspace.sh

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
bind  = $mainMod, mouse:274, killactive,

# Magnifier
$calcZoomInFactor  = echo "$(hyprctl -j getoption cursor:zoom_factor | jq .float) * 1.41421356" | bc
$calcZoomOutFactor = echo "$(hyprctl -j getoption cursor:zoom_factor | jq .float) / 1.41421356" | bc
bind = $mainMod, Page_Up, exec, factor=$($calcZoomInFactor); if echo $factor | grep -E '^\.' >/dev/null 2>&1; then factor=1; fi; hyprctl keyword cursor:zoom_factor $factor
bind = $mainMod, Page_Down, exec, factor=$($calcZoomOutFactor); if echo $factor | grep -E '^\.' >/dev/null 2>&1; then factor=1; fi; hyprctl keyword cursor:zoom_factor $factor
bind = $mainMod, Equal, exec, factor=$($calcZoomInFactor); if echo $factor | grep -E '^\.' >/dev/null 2>&1; then factor=1; fi; hyprctl keyword cursor:zoom_factor $factor
bind = $mainMod, Minus, exec, factor=$($calcZoomOutFactor); if echo $factor | grep -E '^\.' >/dev/null 2>&1; then factor=1; fi; hyprctl keyword cursor:zoom_factor $factor
bind = $mainMod&Control_L, mouse_up, exec, factor=$($calcZoomOutFactor); if echo $factor | grep -E '^\.' >/dev/null 2>&1; then factor=1; fi; hyprctl keyword cursor:zoom_factor $factor
bind = $mainMod&Control_L, mouse_down, exec, factor=$($calcZoomInFactor); if echo $factor | grep -E '^\.' >/dev/null 2>&1; then factor=1; fi; hyprctl keyword cursor:zoom_factor $factor
gesture = 3, up, mod: $mainMod, dispatcher, exec, factor=$($calcZoomInFactor); if echo $factor | grep -E '^\.' >/dev/null 2>&1; then factor=1; fi; hyprctl keyword cursor:zoom_factor $factor
gesture = 3, down, mod: $mainMod, dispatcher, exec, factor=$($calcZoomOutFactor); if echo $factor | grep -E '^\.' >/dev/null 2>&1; then factor=1; fi; hyprctl keyword cursor:zoom_factor $factor

# Screen capture
$grimblast = grimblast --notify save
$cwebp = cwebp -lossless -q 100 -m 6 -mt
bind = ,                  Print, exec, $grimblast screen - | $cwebp -o ~/Pictures/screen_$(date +%Y-%m-%d-%H%M%S).webp -- -
bind = $mainMod,          Print, exec, $grimblast output - | $cwebp -o ~/Pictures/output_$(date +%Y-%m-%d-%H%M%S).webp -- -
bind = Shift_L,           Print, exec, $grimblast active - | $cwebp -o ~/Pictures/active_$(date +%Y-%m-%d-%H%M%S).webp -- -
bind = Control_L,         Print, exec, $grimblast area   - | $cwebp -o ~/Pictures/area_$(date +%Y-%m-%d-%H%M%S).webp   -- -

# Rofi
$findUserDirs = ~/.config/hypr/find-user-dirs.sh
$findPapers = ~/.config/hypr/find-papers.sh
bind = $mainMod&Shift_L, F, exec, $rofi -show window
bind = $mainMod&Shift_L, D, exec, path=$($findUserDirs | $rofi -dmenu -i -p "Open a directory"); if [ -n "$path" ]; then xdg-open "$path"; fi
bind = $mainMod&Shift_L, T, exec, path=$($findUserDirs | $rofi -dmenu -i -p "Open a terminal with a directory"); if [ -n "$path" ]; then $terminal --working-directory "$path"; fi
bind = $mainMod&Control_L, P, exec, path=$($findPapers | $rofi -dmenu -i -p "Open a paper" -theme-str "window { width: 50%; }"); if [ -n "$path" ]; then xdg-open "$HOME/Literature/$path"; fi

bind = $mainMod, D, exec, ~/.config/hypr/show-display-menu.sh

# Mako
# bind  = $mainMod, S, exec, ~/.config/hypr/toggle-notification.sh
# bind  = $mainMod, X, exec, makoctl mode -t expand-group
# bind  = $mainMod, A, exec, makoctl menu rofi -dmenu -p "Mako action: " -theme-str "window { width: 20em; height: 10em; }"
# binde = $mainMod, D, exec, makoctl dismiss
# bind  = $mainMod, U, exec, makoctl restore

# swaync
bind = $mainMod&Control_L, N, exec, swaync-client -t

# Audio control
bind = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bind = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bind = , XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle

# Debug
bind = $mainMod, F12, exec, hyprctl -j activewindow | wl-copy


##############################
### WINDOWS AND WORKSPACES ###
##############################

windowrule = suppress_event maximize, match:class .* # You'll probably like this.

# Make some windows floating by default
windowrule = float on, match:class ^(org.gnome.Calculator)$
windowrule = float on, match:title ^(New Text Note — Okular)$
windowrule = float on, match:class ^(blender-5.0)$
windowrule = float on, match:class ^(wdisplays)$
windowrule = float on, match:class ^(geeqie)$, match:title (Move - Geeqie)
windowrule = float on, match:class ^(nemo)$, match:title ^(.*Properties)$
windowrule = float on, match:class ^(org.inkscape.Inkscape)$, match:title (SVG Input)
windowrule = float on, match:class ^()$, match:title (Picture in picture)  # Chrome's picture in picture window
windowrule = float on, match:class ^(firefox)$, match:title (Picture-in-Picture)  # Firefox's picture in picture window

# Games
windowrule = workspace game, match:class ^(steam)$
windowrule = workspace game, match:title ^(Steam)$
windowrule = tag +game, match:class ^(Minecraft)
windowrule = tag +game, match:class ^(steam_app_1808500)$  # ARC Raiders
windowrule = tag +game, match:class ^(steam_app_949230)$  # Cities: Skylines II
windowrule = tag +game, match:class ^(steam_app_526870)$  # Satisfactory
windowrule = tag +game, match:class ^(steam_app_2489330)$  # Whiskerwood
windowrule {
    name = game-windows
    match:tag = game
    float = on
    fullscreen = on
    content = game
    workspace = game
}

# Firefox's own notification windows
windowrule = tag +firefox-notification, match:class ^(firefox)$, match:title ^()$
windowrule = float on, match:tag firefox-notification
windowrule = move 100%-w-20 20, match:tag firefox-notification
windowrule = pin on, match:tag firefox-notification
windowrule = no_initial_focus on, match:tag firefox-notification
windowrule = suppress_event activate activatefocus, match:tag firefox-notification
windowrule = border_size 0, match:tag firefox-notification

# Modal windows
# Pin entry windows
windowrule = tag +modal, match:class ^(pinentry-.*|hyprpolkitagent|gcr-prompter)$
# File open/save dialogs
windowrule = tag +modal, match:class ^(xdg-desktop-portal-gtk)$
windowrule = tag +modal, match:class ^(firefox)$, match:title ^(Save Image)$
windowrule = tag +modal, match:class ^(mozc_tool)$, match:title ^(export to file|import from file)$
windowrule = tag +modal, match:class ^(nemo)$, match:title ^(Select Target Folder For Move)$
# Rules
windowrule = float on, match:tag modal
windowrule = stay_focused on, match:tag modal
windowrule = dim_around on, match:tag modal

# Idle inhibit rules
windowrule = idle_inhibit focus, match:class ^(mpv)$

# Disable blur
windowrule = no_blur on, match:class ^()$, match:title ^()$  # For Google Chrome's menus

# Layer rules
layerrule = dim_around on, match:namespace rofi
layerrule = blur on, match:namespace rofi
layerrule = blur on, match:namespace waybar
layerrule = dim_around on, match:namespace swaync-control-center


############################
### HOST-SPECIFIC CONFIG ###
############################
source = ~/.config/hypr_host_specific.conf
